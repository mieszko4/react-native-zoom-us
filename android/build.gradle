
buildscript {
    repositories {
        jcenter()
        maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
    buildToolsVersion "26.0.3"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
    lintOptions {
        abortOnError false
    }
}

allprojects {
    repositories {
        flatDir {
            dirs "$rootDir/../react-native-zoom-us/android/libs"
        }
    }
}


dependencies {
    compile 'com.facebook.react:react-native:+' // From node_modules
    compile 'us.zoom.sdk:commonlib@aar'
    compile 'us.zoom.sdk:mobilertc@aar'
}
